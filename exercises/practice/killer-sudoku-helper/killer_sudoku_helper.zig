pub fn combinations(buffer: []u9, sum: usize, size: usize, exclude: u9) []const u9 {
    _ = buffer;
    _ = sum;
    _ = size;
    _ = exclude;
    @compileError("please implement the combinations function");
}
