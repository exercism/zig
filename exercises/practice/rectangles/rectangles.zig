/// Returns the number of rectangles in the ASCII diagram `strings`.
pub fn rectangles(
    strings: []const []const u8
) usize {
    _ = strings;
    @compileError("please implement the rectangles function");
}
